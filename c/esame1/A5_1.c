#include <stdio.h>

int overlap_size (int i, int j, int t, int u)
{
    if(j==t) return 1;
    else
{
    for()
}

}

int main()
{
    int i,j,t,u,intr;
    printf("inscerisci i :");
    scanf("%d", &i);
    printf("inscerisci j :");
    scanf("%d", &j);
    printf("inscerisci t :");
    scanf("%d", &t);
    printf("inscerisci u :");
    scanf("%d", &u);
  
    printf("l'intersezione tra i due insiemi e': %d", overlap_size(i,j,t,u));

}